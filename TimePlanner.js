class TimePlanner{
    constructor(){}

    display(){
        
        var timeText=createElement("h1");
        timeText.position(50,560);
        timeText.html("Time is the most precious gift." +"<br/>"+"Don't waste it.");
              
/*
        var NextButton2=createButton("Let's Begin");
        NextButton2.position(750,800);
        NextButton2.size(100,30);

        NextButton2.mousePressed(function(){

            Text.hide();
            NextButton2.hide();

            game.update(2);
            game.displayChoose();

    
              //  user.updateUserCount(userCount+1);
              //  user.updateUserDetails(name,email);
                
          
        })
 */ 
    }

}